[build]
  command = "npm ci && npx ng build"
  publish = "dist/macro-ng/browser"

[functions]
  directory = "netlify/functions"

# SPA-Fallback — /api/* wird von den Functions VOR den Redirects bedient
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
